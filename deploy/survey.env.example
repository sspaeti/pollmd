# Copy to /usr/local/etc/survey/survey.env and chmod 600.

SURVEY_DB_PATH=/var/db/survey/votes.duckdb
SURVEY_HTTP_ADDR=127.0.0.1:8080
SURVEY_QUACK_ADDR=127.0.0.1:9494
SURVEY_BLOG_URL=https://www.ssp.sh

# Generate with: head -c 32 /dev/urandom | base64
SURVEY_QUACK_TOKEN=CHANGE_ME_TO_BASE64_TOKEN
